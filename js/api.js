//the call backs for backend
// Might be able to use a .JSON file in leu of a proper database.
/*
class API {
    constructor() {}
    
    // Returns the scenario name based on the scenario ID given.
    getScenName(scenID) {
        //
    }

    // Returns the scenario description based on the scenario ID given.
    getScenDesc(scenID) {
        //
    }

    // Returns the maximum amount of point that can be gotten for the specified scenario.
    getMaxPoints(scenID) {
        //
    }

    // Returns the points associated with the selection based on the specified scenario.
    getSelectionPoints(scenID, selection) {
        //
    }

    // Adds an entry to a table that will store completed scenarios.
    addCompletedScenario(playerID, scenarioID, selected, points) {
        //
    }

    // Checks to see if the given username and password exist in the database. Returns true if they are.
    checkUsernamePassword(username, password) {
        //
    }

    // Determines if the account tied to the username is a player account or not (admin). Returns true if so.
    isPlayer(username) {
        //
    }
}
*/